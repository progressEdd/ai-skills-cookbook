---
type: daily-note
---
# 2026-08-26
## Overall Progress
- [ ] Committed the 08/25 docs-setup research into the dev log (aihero.dev is a custom Next.js app; Software Mansion uses Astro + Starlight)
- [ ] Recovered from an accidental push to `master` — moved all work to a new `progressedd` branch and reset `master` to the blank template
- [ ] Refined [core ideas](core-ideas.md) — added context management and a "review the harnesses / choose your workflow" capstone
- [ ] Scaffolded the course in the `02-worktrees/course` worktree (`00-start-here` + lessons `01`–`08`)
- [ ] Wrote the git crash course — adapted `01-dev-onboarding`'s intro-to-git, added a cwd/pwd/cd primer, git-and-paths notes, undo toolbox, worktrees, and merge/rebase
- [ ] Restructured all 9 lessons: inline **Your turn** exercises after each concept, trailing `## Exercises` replaced by `## Checkpoint`
- [ ] Built the course's branch topology: `master` as public baseline, minimal `starter-skills` kit branch with a simulated "teammate" conflict, `course`, `blank-branch` — all pushed
- [ ] Rewrote the root README's Getting Started around a platform-agnostic, per-clone `setup-worktrees` git alias
- [ ] Worktree housekeeping — added `master` and `blank-branch` worktrees; brief `00-dev-log` submodule experiment (added, then removed)

## Elaboration into overall progress

### Accidental push recovery → branch strategy
- 10 commits had been pushed to `origin/master` by mistake. Created `progressedd` from that tip and pushed it, then force-pushed `master` back to the blank template commit (`f45de35`: `.gitignore`, `LICENSE`, `README.md`)
- End state: `master` stays the blank, reusable template for anyone cloning; ongoing work lives on `progressedd`

### Core ideas refinements
- Added **Context management** checklist item (what goes in the context window, excluding noise, when to start a fresh session)
- Added capstone item: reviewing `00-supporting-files/harnesses/` to choose your own workflow — doubling as deliberate context-management practice (`@`-referencing one file vs. letting the agent explore a whole folder)
- Surveyed the five harness repos (skills, openspec, gsd-core, gsd-pi, microsoft-agentic-harness) as a spectrum of "how much process the agent owns"

### Course scaffolding (`02-worktrees/course`)
- One stub per core idea, flat with numeric prefixes for reading order (`01-git-crash-course.md` … `08-choose-your-workflow.md`), plus `00-start-here.md` (install git + a harness — pi preferred, Cursor/Claude Code as alternatives) and a real course README
- Every lesson got the same light scaffold: quote-tagline, checkable learning objectives, concept headers with `<!-- TODO -->` markers, exercises, further-reading stub
- Later restructured so each concept is followed immediately by a **Your turn** exercise; a lesson's end (`## Checkpoint`) now means every box is checked
- `01-git-crash-course.md` adapted from `01-dev-onboarding/02-dev-workflows/intro-to-git.md`: kept the workflow steps, worktrees section, and glossary (condensed); added "why git matters more with agents", an undoing-things section, a cross-platform cwd primer (`pwd` vs bare `cd` in cmd, `~` vs `%USERPROFILE%`), a "Git and paths" note (verified empirically: `git add` resolves cwd-relative, `git status` renders location-relative, `git add -A` stages the whole repo), and the student's `my-course` worktree where checkbox progress becomes git history

### Course branch topology (all pushed to origin)
- `master` — replicated the `progressedd` structure (README, AGENTS.md, `.gitmodules` + 6 submodule gitlinks, `.foam/`, `02-worktrees/README.md`) but without dev-log contents; `.agents/` starter kit moved off it (`6292053`)
- `starter-skills` — rebuilt minimal (blank-branch base + `.agents/` kit only, `02ad4fc`) with a "teammate" commit rewording the README tagline (`7cc67ca`) so students hit a real, guided conflict during the merge step
- `course` — lessons + README with the worktree setup; restructured in `26c09c8`, merge/rebase walkthrough in `469955f`, merge-vs-rebase mechanics in `0915aa7`
- `blank-branch` — pushed so clones can use it as a blank canvas
- The full student flow was simulated end-to-end from a fresh clone; that surfaced a real gotcha now documented in the course: `git worktree add <path> -b <name> <remote-only-branch>` silently drops `-b` — spell the source as `origin/<branch>`
- Brief `00-dev-log` submodule experiment on `master` (seed repo + gitlink, `1a58747`) — removed again (`763b9a2`) after concluding master doesn't need it

### Root README / worktree setup
- Getting Started is now a single flow: install git → clone with submodules → install a **per-clone** `setup-worktrees` git alias → run it (creates a worktree per remote branch under `02-worktrees/`, skipping the already-checked-out default)
- The alias is platform-agnostic because `!`-prefixed aliases run through git's bundled POSIX shell (works in cmd, PowerShell, zsh…); tested end-to-end in a scratch repo, idempotent, with an explicit Windows cmd install variant
- Also investigated: pi's `find`/`grep` respect `.gitignore` (so worktree contents are hidden from repo-wide search) while direct `@`-access still works; a `.ignore` re-include layer was tried and then discarded
- `02-worktrees/master` and `02-worktrees/blank-branch` added locally; the worktrees README updated to list them (blank-branch entry pending commit)
