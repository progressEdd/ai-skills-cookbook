---
type: daily-note
---
# 2026-08-29
## Overall Progress
- [ ] Threaded the kitchen analogy through the entire course — reworked the mental model in `00-start-here.md`, added an at-a-glance table, and "Back to the kitchen" callouts to every lesson (`3fae728`)
- [ ] Fixed the broken lesson link in the course README (`02-navigating-the-chat` → `02-navigating-the-harness`, `205b1a7`)
- [ ] Recovered from an accidental overwrite of lesson 02 — restored from git, re-applied the kitchen callout, and discarded the `/tmp` backup on request
- [ ] Added a local-LLM "operate your own shuttle" option to the transportation analogy (alongside per-token fares and monthly bus passes)
- [ ] Built a Foam course-lesson template (`.foam/templates/course-template.md`, `c1ea6ed`) and debugged Foam's `FOAM_CURRENT_DIR` resolution in the installed extension's bundled source
- [ ] Normalized all 9 lessons to follow the Foam template — canonical intro line, checkpoint wording with next-lesson links, kitchen blockquote added to the template itself (landed in `65029de`)
- [ ] Drafted lesson 02 (`@` references and `/` commands) — user-written content landed via `65029de`
- [ ] Researched how pi handles `.gitignore` (grep honors it via ripgrep; explicit reads and shell commands don't) — undocumented behavior traced through pi's source

## Elaboration into overall progress

### Kitchen analogy threaded through the course (`3fae728`)
- Reviewed the full course for where the kitchen analogy (skills = recipes, scripts = utensils) could extend, then applied it across 10 files: `00-start-here.md`'s section renamed to "**The kitchen: a mental model for vibe coding**" with a new learning objective and an at-a-glance summary table moved to the end of the content; "Back to the kitchen" callouts added under `## Concepts` in lessons 01–08; README link fix plus analogy note
- The README's broken-link fix was isolated into its own commit (`205b1a7`) — during staging, a stray one-word tweak (`so you can` → `so that you can`) was caught sneaking into the link-fix commit and amended out before committing the analogy thread separately (`3fae728`)

### Lesson 02 overwrite recovery
- An accidental editor overwrite clobbered `02-navigating-the-harness.md` with a different draft; since the pre-overwrite version (lesson stub + new callout) was never committed, recovery was: back up the overwritten content to `/tmp`, `git restore`, then re-apply the callout by hand
- The overwritten draft contained real content (a "stored recipes" tagline, filled-in objectives, concept headings) that was later re-created by hand and eventually landed in `65029de`; the `/tmp` backup was deleted once it was clear the re-write superseded it

### Local-LLM shuttle analogy
- The transportation metaphor for model access gained a third option: operating your own shuttle (a local LLM via Ollama/LM Studio) — no fares or schedule (no per-token cost, works offline), full privacy (nothing leaves your property), but you buy and maintain the shuttle yourself (hardware, setup, weaker chefs)

### Foam course-lesson template (`c1ea6ed`)
- Rewrote `.foam/templates/course-template.md` to generate lessons matching the `00-start-here.md` pattern: front matter (`type`, `name`, `description`, `filepath`), `$FOAM_TITLE` prompt, and tab-stop placeholders for objectives, kitchen-analogy concepts, **Your turn** exercises, and the checkpoint
- `filepath` iteration: dropped it (to prompt for a folder) → set `$FOAM_CURRENT_DIR/$FOAM_SLUG.md` (save next to the open file) → hit a real quirk: the note dumped into workspace root. Traced Foam 0.44.5's bundled extension code: `resolveCurrentDir()` reads `window.activeTextEditor` at creation time and **falls back to the first workspace folder** if no editor has keyboard focus — so the fix is a habit change (click into the editor before running the template command), not a template change

### Course normalized to the template (in `65029de`)
- All 9 lessons now follow the template: canonical "Each concept below ends with a **Your turn**…" intro line everywhere, checkpoint clauses plus next-lesson links (01 and 02 were missing links; 02's checkpoint was still an HTML TODO), and the template gained the recurring `> **Back to the kitchen:` blockquote
- Follow-up discussion tightened lesson 01's intro into a single four-sentence reading contract (collapsing a doubled "run before moving on" / "type as you read" opener) and moved its kitchen blockquote directly under `## Concepts` to match lessons 02–08; decided against replacing the lesson epigraph with the analogy — the epigraph carries the lesson's thesis
- Committed on `course` as `65029de` (9 files, +99/−28), which also carried the lesson 02 draft (user-written outside agent sessions) and the shuttle analogy; the Foam template committed separately on `progressedd` as `c1ea6ed` (still unpushed — branch is ahead 1)

### pi + `.gitignore` research
- Answered for lesson-02 research: pi's `grep` tool runs ripgrep with `--hidden`, so it honors `.gitignore` (plus `.ignore`/`.rgignore`) — but explicit `read`s and shell commands bypass it entirely; confirmed this behavior appears nowhere in pi's docs, only in tool schemas and source

### Session notes
- All work came from pi sessions (glm-5.2), 13 sessions total — including a batch of eight one-shot Q&A sessions (15:39–16:07) all asking about the repo's license (GPL-2.0, with a duplicate copy in `01-dev-onboarding/`), which look like repeated phrasing/model tests
- The current uncommitted edits to `02-navigating-the-harness.md` (a new slash-command section) are from 08/30 and belong to tomorrow's note
