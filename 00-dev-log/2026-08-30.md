---
type: daily-note
---
# 2026-08-30
## Overall Progress
- [ ] Landed the lesson-02 slash-command section flagged in yesterday's note as "belonging to tomorrow" (`358e91c`, "02 navigating the harness complete")
- [ ] Renamed the `skills` submodule to `mattpocock-skills` — repaired the stale index gitlink, moved the absorbed gitdir, and purged 12 leftover `submodule.*` config sections (`7669794`)
- [ ] Drafted lesson 03's "What is AGENTS.md?" section (convention, tool variants, this repo's AGENTS.md as example, OpenAI's context-rot learning) plus further-reading links (`b62598b`)

## Elaboration into overall progress

### Submodule rename to mattpocock-skills (`7669794`)
- `.gitmodules` had already been edited to the new path, but the index gitlink, on-disk directory, and `.git/config` all still referenced `00-supporting-files/harnesses/skills` — `git submodule status` failed with "no submodule mapping found"
- Fix: renamed the worktree dir and its `.git/modules/.../skills` gitdir, repaired the submodule's `.git` pointer file and `core.worktree` (edited the module config directly — `git config` itself choked on the stale worktree path), then swapped the index gitlink (`git rm --cached` old → `git add` new), which git detected as a 100% rename at the same pinned commit `6654f6b`
- `.git/config` had accumulated stale `submodule.*` sections from older path layouts (`00-supporting-files/skills`, `harnesses/*`, `00-supporting-files/harnesses/skills`) — removed all and regenerated a clean set with `git submodule init` + `sync`; all 6 submodules now report healthy
- Follow-up: `06-customizing-skills.md` in the `course` worktree still referenced the old `harnesses/skills/` path twice — resolved same day (`2516680`)

### Lesson 03: AGENTS.md section drafted (`b62598b`)
- Filled the "What is AGENTS.md?" TODO: the agents.md "README for agents" framing, tool-specific variants (`CLAUDE.md`, `CURSOR.md`, `HERMES.MD`), this repo's actual AGENTS.md quoted as an example, and OpenAI's harness-engineering learning that splitting rules into `.agents/` (with AGENTS.md as a table of contents) reduces context rot
- Split the Your-turn exercise into two items and linked mattpocock-skills' AGENTS.md as a concrete example — the link only works because of the rename above; added five further-reading links (agents.md, pi, Codex, OpenAI, Cursor) and fixed the file's missing trailing newline before committing

### Session notes
- All work came from pi sessions (glm-5.2); the lesson-03 content itself was user-written, the agent handled the commit and the submodule repair
- The lesson-02 edits flagged in yesterday's note as "belonging to tomorrow" indeed landed today in `358e91c`
- `progressedd` remains unpushed and ahead of origin (submodule rename commit `7669794`); the lesson-03 commit `b62598b` is on `course` in `02-worktrees/`
